# Contact form endpoint (Formspree / Web3Forms / any service that accepts a POST of form fields).
# Create a free form at https://formspree.io and paste its URL here, then copy this file to `.env`.
# Leave empty to fall back to opening the visitor's email app (mailto:).
PUBLIC_FORM_ENDPOINT=
