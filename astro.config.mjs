import { defineConfig } from 'astro/config';

// `site` is used for the canonical URL, Open Graph tags and the sitemap-ready absolute links.
// If you deploy to a GitHub Pages *project* URL (https://<user>.github.io/<repo>/) instead of the
// custom domain, also set `base: '/<repo>'`.
export default defineConfig({
  site: 'https://shreedrt.com',
});
